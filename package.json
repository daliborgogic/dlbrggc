{
  "name": "daliborgogic.com",
  "version": "1.0.0",
  "description": "###",
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "lint": "eslint --ext .js,.vue --ignore-path .gitignore ."
  },
  "devDependencies": {
    "babel-eslint": "^10.0.2",
    "date-fns": "^2.0.0-beta.3",
    "eslint": "^5.16.0",
    "eslint-loader": "^4.0.2",
    "eslint-plugin-vue": "^4.7.1",
    "nuxt": "^3.0.0",
    "r2": "^2.0.1",
    "stylus": "^0.54.5",
    "stylus-loader": "^7.1.0"
  },
  "eslintConfig": {
    "root": true,
    "env": {
      "browser": true,
      "node": true
    },
    "parserOptions": {
      "parser": "babel-eslint"
    },
    "extends": [
      "eslint:recommended",
      "plugin:vue/recommended"
    ],
    "plugins": [
      "vue"
    ],
    "rules": {
      "semi": [
        2,
        "never"
      ],
      "no-console": "error",
      "vue/max-attributes-per-line": "off"
    }
  },
  "dependencies": {
    "@nuxtjs/style-resources": "^0.1.2"
  }
}
